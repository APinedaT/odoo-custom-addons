<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_generate_electronic_invoice_from_pos_orders" model="ir.actions.server">
        <field name="name">Generate Electronic Invoice from POS Orders</field>
        <field name="model_id" ref="point_of_sale.model_pos_order"/>
        <field name="binding_model_id" ref="point_of_sale.model_pos_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_many_pos_orders_einvoices()</field>
    </record>
</odoo>
